For running:
    0.Activate virtualenv.
    1.Install all necesary dependencies from requirements.txt with pip.
    2.Fill a 'ProductionConfiguration' in config.py for deployment.
    3.Run with (export FLASK_APP="run";export FLASK_ENV="development"; flask run)